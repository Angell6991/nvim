
revisar el git-hub de packer y instalar con git clone

comando nesesario en nvim en el documento packer.lua

:so
:PackerSync 

usar en la linea de comandos 

:checkhealth telescope  

para berificar los programas faltantes para telescope
en general es live-grep pero tambien se puede instalar
como sustituto ripgrep:

sudo pacman -S  ripgrep
sudo pacman -S  live-grep

